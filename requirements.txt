# ── Core data & ML ────────────────────────────────────────────────────────────
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
matplotlib>=3.8
tqdm>=4.66
joblib>=1.3

# ── Deep Learning (inference.py + src/main_simcprs*.py) ───────────────────────
torch>=2.9
transformers>=4.40
datasets>=2.18

# ── Learning-to-Rank (train_l2r.py) ───────────────────────────────────────────
lightgbm>=4.3
xgboost>=2.0

# ── Embedding-based similarity (compute_sim_features.py --sim_method embed) ───
sentence-transformers>=3.0

# ── Optional: TensorFlow (src/main_fpsrs.py only) ─────────────────────────────
# tensorflow>=2.15
